- 노드 이동, 텍스트 편집, 연결 변경, 정렬 기록 미지원 -> 지원 필요
- 노드 삭제 시 키 삭제 안되는 문제
- 여러 상황에서의 정렬 문제
  - 자동 위치 계산: 신규 노드나 우측에 바로 생성하는 노드의 경우 위치 이상함
  - 자동 정렬도 이상함
- 정렬 버튼 일원화 (전체 정렬의 알고리즘을 dagre로 변경하면 되지, darge 정렬을 따로 버튼으로 제공할 필요가 없음)
- 여러 노드 복사시에 연결관계 복사 안되는 문제
- 노드 속성 설정 패널에서 노드 타입 변경 지원
- 우측 핸들 클릭으로 노드 생성/연결할 때, 화자 동일한 값으로 자동 입력해두기
- 순환형 연결이 있다면 자식 노드 정렬 하면 무한루프에 들어가는 것으로 보임. (화면이 실제로 멈추고 응답이 없어짐)
- 토스트 메세지 중앙하단에서 중앙 상단으로 위치 변경
- key 편집 후 사용 개수 업데이트 필요 (1개인 키 변경하니 0개로 표시)
- 삭제 경고 없애기 (undo 생겼으니까)
- 노드 우클릭 메뉴 지원
- 되돌릴 내용이 없을 때 undo 하면 토스트 메세지? (근데 다른 툴에서는 굳이 안하긴 함 + 복구 성공했을 때도 잘 안하긴 함)
- 정렬 후 캔버스 뷰 이동 필요 (노드가 갑자기 사라진 것처럼 보임)